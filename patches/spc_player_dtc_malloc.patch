--- a/spc_player.c
+++ b/spc_player.c
@@ -1189,7 +1189,7 @@ static void Interrupt_Reset(SpcPlayer *p) {
 }
 
 SpcPlayer *SpcPlayer_Create() {
-  SpcPlayer *p = (SpcPlayer *)ahb_malloc(sizeof(SpcPlayer));
+  SpcPlayer *p = (SpcPlayer *)dtc_malloc(sizeof(SpcPlayer));
   p->dsp = dsp_init(p->ram);
   p->reg_write_history = 0;
   return p;
